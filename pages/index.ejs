<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <title><%= title %></title>
</head>
<body>
  
  <% if(created) {%>
  <div class="alert alert-success">
    Заметка создана
  </div>
  <% } %>

  <% if(visible) {%>
  <div class="container">
    <div class="modal_window border p-2 rounded position-relative">
      <h1>Введите новое название</h1>
      <form action="/:id" method="post">
        <input type="text" name="new-title" id="new-title" class="form-control">
        <div class="mt-2">
          <button class="btn btn-primary">Ok</button>
          <button class="btn btn-light">Cancel</button>
        </div>
      </form>
    </div>
    <% } %>


    <h1>Список заметок</h1>
    <form action="/" method="post" class="mb-2">
      <div class="form-group mb-2">
        <input type="text" name="title" id="title" class="form-control">
      </div>
      <button class="btn btn-primary" type="submit">Создать</button>
    </form>
    <% if (notes.length) {%>
    <div class="card">
      <ul class="list-group">
        <% for (let i = 0; i < notes.length; i++) { %>
        <li class="list-group-item d-flex justify-content-between align-items-center">
          <%= notes[i].title %>
          <div class="buttons">
            <button class="btn btn-danger" data-type="remove" data-id="<%= notes[i].id %>">x</button>
            <button class="btn btn-danger" data-type="edit" data-id="<%= notes[i].id %>">edit</button>
          </div>
        </li>
        <% } %>
      </ul>
    </div>
    <%} else {%>
      <p>Нет заметок!</p>
    <% } %>
  </div>
  <script src="/app.js"></script>
</body>
</html>
